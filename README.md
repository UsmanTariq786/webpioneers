This is a [Next.js](https://nextjs.org) project bootstrapped with [`create-next-app`](https://nextjs.org/docs/app/api-reference/cli/create-next-app).

## Getting Started

First, run the development server:

```bash
npm run dev
# or
yarn dev
# or
pnpm dev
# or
bun dev
```


## Env file constatns: 

EMAIL_USER                  => its the email of client 
EMAIL_PASS                   => its the passcode of client of above mail which can be used for nodemailder

CLIENT_EMAIL                  => on this mail form data would be sent 
CLIENT_EMAIL_2                 => on this mail form data would be sent